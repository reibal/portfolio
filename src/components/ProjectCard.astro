---
const { title, description, link, image } = Astro.props
---

<article class="border border-white rounded bg-gradient-to-br from-slate-600/20 to-slate-800/30">
	<div class="px-4 py-3">
		<h2 class="text-orange-50 text-2xl font-bold">{title}</h2>
		<p class="my-1">{description}</p>
	</div>
	<img
	alt=`Image of the project ${title}`
	src={image}
	/>
</a>